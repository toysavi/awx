[defaults]
inventory           = ansible/inventory
roles_path          = ansible/playbook:ansible/awx-deployment/awx_roles
host_key_checking   = False
retry_files_enabled = False
stdout_callback     = yaml
timeout             = 30

[privilege_escalation]
become        = True
become_method = sudo
