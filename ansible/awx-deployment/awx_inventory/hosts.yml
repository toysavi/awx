---
# AWX-deployment-specific inventory (overrides global if used standalone)
all:
  vars:
    ansible_user: root
    ansible_ssh_private_key_file: ~/.ssh/id_rsa

k3s_masters:
  hosts:
    awx-master-01:
      ansible_host: 192.168.1.10
      ip: 192.168.1.10

k3s_workers:
  hosts: {}
